devtools::install_github(repo = "CHEN7019/xkcd",host = "github.umn.edu/api/v3",build_vignettes = TRUE,force=T,auth_token = "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")
library(xkcd)
help(xkcd)
try(a <- 1,{a <- -1; cat("GG")})
a
try(a <- 1/0,{a <- -1; cat("GG")})
a
try(a <- b,{a <- -1; cat("GG")})
b
A <- matrix(c(1,0,0,0),nrow=2)
A
solve(A)
try(res <- solve(A),{res <- solve(A+1e-6*diag(2))})
res
try(res <- solve(A),{res <- solve(A+1e-6*diag(2)),cat("GG")})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat("GG")})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2))})
res
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat("?")})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat("GG")})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2))})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));res<-diag(3)})
res
help(cat)
cat("?")
A <- matrix(c(1,0,0,0),nrow=2)
try(res <- solve(A),{res <- solve(A+1e-6*diag(2))})
res
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));res<-diag(3)})
res
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat("GG")})
res
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat(GG)})
try(res <- solve(A),{res <- solve(A+1e-6*diag(2));cat("GG")})
